# ── Lakeflow Designer — Port Config ───────────────────────────────────────────
# Change these if something else is already using the ports.
# After changing, just run ./run.sh again.

FRONTEND_PORT=3000
BACKEND_PORT=8000
